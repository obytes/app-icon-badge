{
  "name": "app-icon-badge",
  "license": "MIT",
  "version": "0.0.1",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "assets"
  ],
  "scripts": {
    "build": "tsup index.ts --format cjs,esm --dts && tsup expo-plugin.ts --format cjs,esm --dts",
    "release": "pnpm run build && changeset publish",
    "start": "ts-node example.ts",
    "type-check": "tsc --noEmit",
    "preinstall": "npx only-allow pnpm",
    "np": "pnpm build && np --no-cleanup ",
    "test": "echo \"no test specified\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/obytes/app-icon-badge.git"
  },
  "author": "Siham Ben Daia",
  "bugs": {
    "url": "https://github.com/obytes/app-icon-badge/issues"
  },
  "homepage": "https://github.com/obytes/app-icon-badge#readme",
  "dependencies": {
    "jimp": "^0.22.7"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "prettier": "2.8.7",
    "ts-node": "^10.9.1",
    "tsup": "^6.5.0",
    "typescript": "^4.9.4",
    "np": "^7.7.0"

  }
}
